# AI-Invoice-Processing-Pipeline
Automates invoice processing using vision-capable AI — extracts structured data (vendor, amount, date, line items) from invoice documents and turns them into clean outputs.
